# DEEPEN-LOG — MiniPIM

- 2026: Port nghiệp vụ **Product Master enrichment** từ `2019.4.ProductCenter` (TblMst_Product / Mst_Attribute): thêm danh mục thuộc tính dùng chung (AttributeDef) + các trường master sản phẩm (ProductLevelSys→Level, ValConvert, QtyMinSt/QtyMaxSt, VATRateCode, FlagSerial/FlagLot, ProductOrigin, ProductQuyCach) + quy tắc nghiệp vụ "thành phần BOM không được quản lý serial/lô" (Mst_Product_Create_Invalid_Prd_BOM_FlagSerialOrFlagLot). Thêm màn /Attribute, mở rộng form Product, API /api/products/{code} trả thêm trường master. Build Release 0 error, smoke test /Attribute=200 + API OK.
- 2026: Port nghiệp vụ **Quy cách + Bảng giá theo quy cách** từ `2019.4.ProductCenter` (Mst_Spec / Mst_SpecPrice — phần "quy cách, giá" của product master chưa có trong MiniPIM): thêm entity `Spec` (SpecCode/SpecName/ModelCode/SpecType1-2/Color/FlagHasSerial/FlagHasLOT/DefaultUnitCode/StandardUnitCode) và `SpecPrice` (khóa nghiệp vụ SpecCode+UnitCode, BuyPrice/SellPrice/CurrencyCode/VATRateCode/DiscountVND/EffectDTimeStart-End). Quy tắc nghiệp vụ port từ `Mst_SpecPrice_Create`/`Mst_SpecPrice_CheckDB`: quy cách phải tồn tại & đang dùng, mỗi cặp (SpecCode, UnitCode) chỉ 1 dòng giá, giá không âm. Thêm màn /Spec (CRUD quy cách) + /SpecPrice (bảng giá, lọc theo quy cách), API `/api/specs` và `/api/specs/{code}/prices`. Build Release 0 error; smoke test /Spec=200, /SpecPrice=200, API OK, và POST trùng giá bị chặn đúng thông báo.
- 2026: Port nghiệp vụ **Nhóm hàng phân cấp** từ `2019.4.ProductCenter` (Mst_ProductGroup — phần "danh mục" của product master chưa có trong MiniPIM, trước đây `ProductGroup` chỉ là 1 tầng phẳng): thêm trường phân cấp `ParentCode` + đường dẫn vật chất hóa `BUCode`/`BUPattern`/`Level` + `FlagFG`/`Active`. Quy tắc nghiệp vụ port từ `Mst_ProductGroup_CreateX`/`UpdateX`/`DeleteX`/`UpdBU`: mã & tên nhóm bắt buộc, mã/tên không trùng, nhóm cha phải tồn tại, không cho nhóm là cha của chính nó; sau mỗi lần lưu/xóa tự tính lại đường dẫn BU (gốc = mã, con = "cha.con", Level = cha.Level+1); không cho xóa nhóm nếu còn hàng hóa thuộc nhóm hoặc còn nhóm con. Thêm màn /Group (cây phân cấp + form Edit chọn nhóm cha) và nút xóa có guard. Build Release 0 error (đồng thời loại các file dump `_*.cs` khỏi compile trong csproj — chúng là bản sao nguồn ProductCenter dùng để đọc, không thuộc app và đang làm hỏng build).
- 2026: Port nghiệp vụ **Danh mục Đơn vị tính** từ `2019.4.ProductCenter` (Mst_Unit — danh mục ĐVT dùng chung cho sản phẩm/quy cách/bảng giá, trước đây MiniPIM chỉ lưu `Uom` dạng chuỗi tự do, chưa có danh mục): thêm entity `Unit` (Code = UnitCode hệ thống tự sinh, CodeUser = UnitCodeUser người dùng nhập, Name = UnitName, Remark, FlagActive→Active). Quy tắc nghiệp vụ port từ `Mst_Unit_CreateX`/`Mst_Unit_UpdateX`/`Mst_Unit_DeleteX`/`Mst_Unit_CheckDB_New20240108`/`Mst_Unit_CheckUnitCodeUser`/`Mst_Unit_CheckUnitName`: mã người dùng & tên ĐVT bắt buộc, mã hệ thống/mã người dùng/tên đều duy nhất trong tổ chức, khi sửa ĐVT phải tồn tại, khi xóa ĐVT phải tồn tại. Thêm màn /Unit (danh sách + tìm kiếm + form thêm/sửa/xóa), seed 6 ĐVT mẫu (Cái/Chiếc/Bộ/Kg/Mét/Thùng), thêm bảng `Units` vào migration Postgres. Build Release 0 error.- 2026: Port nghiệp vụ **Danh mục Thuế suất GTGT** từ `2019.4.ProductCenter` (Mst_VATRate — danh mục dùng chung cho product master, trước đây MiniPIM chỉ lưu `VatRateCode` dạng chuỗi tự do ở Product/SpecPrice, chưa có danh mục): thêm entity `VatRate` (Code = VATRateCode, Rate = VATRate %, Name = VATDesc, FlagActive→Active). Quy tắc nghiệp vụ port từ `Mst_VATRate_CheckDB`/`Mst_VATRate_Create`/`Mst_VATRate_Update`/`Mst_VATRate_Delete` (đọc từ nguồn C# + bảng mã lỗi ErrProductCenter): mã thuế suất & tên bắt buộc, mã không trùng, giá trị không âm, khi sửa/xóa phải tồn tại. Thêm màn /VatRate (danh sách + tìm kiếm + form thêm/sửa/xóa), seed 4 thuế suất mẫu (VAT0/VAT5/VAT8/VAT10), thêm bảng `VatRates` vào migration Postgres. Build Release 0 error.
- 2026: Port nghiệp vụ **Danh mục Model / Dòng sản phẩm** từ `2019.4.ProductCenter` (Mst_Model — danh mục dùng chung cho product master chưa có trong MiniPIM, trước đây `Spec.ModelCode` chỉ là chuỗi tự do, chưa có danh mục model): thêm entity `Model` (Code = ModelCode, Name = ModelName, OrgModelCode, BrandCode = nhãn hiệu Mst_Brand, NetworkModelCode = mã model chung network, Remark, FlagActive→Active). Quy tắc nghiệp vụ port từ `Mst_Model_CheckDB`/`Mst_Model_Create`/`Mst_Model_Update`/`Mst_Model_Delete` (đọc từ nguồn C# BizProductCenter + bảng mã lỗi ErrProductCenter): mã model & tên model bắt buộc, mã không trùng trong tổ chức, nhãn hiệu (BrandCode) bắt buộc và phải tồn tại & đang dùng (Mst_Brand_CheckDB), khi sửa/xóa model phải tồn tại. Thêm màn /Model (danh sách + tìm kiếm + form thêm/sửa/xóa, chọn nhãn hiệu), seed 2 nhãn hiệu mẫu (BRAND-A/BRAND-B) + 3 model mẫu (AO-001/QUAN-001/PK-001), thêm bảng `Models` vào migration Postgres. Build Release 0 error.- 2026: Port nghiệp vụ **Danh mục Loại hàng hóa** từ `2019.4.ProductCenter` (Mst_ProductType — danh mục dùng chung cho product master chưa có trong MiniPIM, trước đây `Product` không có trường loại hàng hóa): thêm entity `ProductType` (Code = ProductType, Name = ProductTypeName, Remark, FlagActive→Active) + trường `Product.ProductTypeCode` tham chiếu loại hàng hóa. Quy tắc nghiệp vụ port từ `Mst_ProductType_CheckDB`/`Mst_ProductType_Create`/`Mst_ProductType_UpdateX`/`Mst_ProductType_Delete` (đọc từ nguồn C# BizProductCenter + bảng mã lỗi ErrProductCenter): mã loại & tên loại bắt buộc, mã không trùng trong tổ chức, khi sửa/xóa phải tồn tại, không cho xóa nếu loại đang được hàng hóa sử dụng. Thêm màn /ProductType (danh sách + tìm kiếm + form thêm/sửa/xóa), seed 3 loại mẫu (NORMAL/COMBO/SERVICE — COMBO theo TConst.ProductType.Combo), API `/api/product-types`, thêm bảng `ProductTypes` + cột `Products.ProductTypeCode` vào migration Postgres. Build Release 0 error.- 2026: Port nghiệp vụ **Danh mục Tỷ giá ngoại tệ** từ `2019.4.ProductCenter` (Mst_CurrencyEx — danh mục dùng chung cho product master chưa có trong MiniPIM, trước đây `SpecPrice.CurrencyCode` chỉ là chuỗi tự do, chưa có danh mục ngoại tệ/tỷ giá): thêm entity `CurrencyEx` (Code = CurrencyCode, Name = CurrencyName, BaseCurrencyCode = đồng tiền gốc quy đổi, BuyRate/SellRate = tỷ giá mua/bán, InterEx = tỷ giá liên ngân hàng, Remark, FlagActive→Active). Quy tắc nghiệp vụ port từ `Mst_CurrencyEx_Create`/`Mst_CurrencyEx_Update`/`Mst_CurrencyEx_Delete`/`Mst_CurrencyEx_CheckDB` (đọc từ nguồn C# BizProductCenter + bảng mã lỗi ErrProductCenter): mã ngoại tệ & tên bắt buộc, mã không trùng trong tổ chức, đồng tiền gốc (BaseCurrencyCode) nếu có phải tồn tại (Mst_CurrencyEx_CheckDB_CurrencyCodeNotFound), khi sửa/xóa phải tồn tại. Thêm màn /CurrencyEx (danh sách + tìm kiếm + form thêm/sửa/xóa), seed 3 ngoại tệ mẫu (VND/USD/EUR), thêm bảng `CurrencyExes` vào migration Postgres. Build Release 0 error.
- 2026: Port nghiệp vụ **Đơn vị tính theo quy cách** từ `2019.4.ProductCenter` (Mst_SpecUnit — phần "quy cách" của product master chưa có trong MiniPIM, trước đây quy cách chỉ có 1 ĐVT mặc định/chuẩn dạng chuỗi, chưa có bảng nhiều ĐVT bán được kèm hệ số quy đổi & kích thước đóng gói): thêm entity `SpecUnit` (khóa nghiệp vụ SpecCode+UnitCode, StandardUnitCode = ĐVT chuẩn quy đổi về, SpecUnitDesc→Description, Qty = hệ số quy đổi, Length/Width/Height/Volume/Weight = kích thước & khối lượng đóng gói, Remark, FlagActive→Active). Quy tắc nghiệp vụ port từ `Mst_SpecUnit_Create`/`Mst_SpecUnit_Update`/`Mst_SpecUnit_Delete`/`Mst_SpecUnit_CheckDB` (đọc từ nguồn C# BizProductCenter + bảng mã lỗi ErrProductCenter): quy cách (SpecCode) và ĐVT (UnitCode) bắt buộc và phải tồn tại & đang dùng (Mst_Spec_CheckDB / Mst_Unit_CheckDB), ĐVT chuẩn nếu có phải tồn tại & đang dùng, mỗi cặp (SpecCode, UnitCode) chỉ 1 dòng, hệ số quy đổi không âm, khi sửa/xóa phải tồn tại. Thêm màn /SpecUnit (danh sách lọc theo quy cách + form thêm/sửa/xóa, chọn quy cách & ĐVT), seed 3 dòng mẫu (AO-001-DENIM-M theo Cái/Thùng, QUAN-001-DENIM-32 theo Cái), thêm bảng `SpecUnits` vào migration Postgres. Build Release 0 error.
