# DEEPEN-LOG — MiniPIM

- 2026: Port nghiệp vụ **Product Master enrichment** từ `2019.4.ProductCenter` (TblMst_Product / Mst_Attribute): thêm danh mục thuộc tính dùng chung (AttributeDef) + các trường master sản phẩm (ProductLevelSys→Level, ValConvert, QtyMinSt/QtyMaxSt, VATRateCode, FlagSerial/FlagLot, ProductOrigin, ProductQuyCach) + quy tắc nghiệp vụ "thành phần BOM không được quản lý serial/lô" (Mst_Product_Create_Invalid_Prd_BOM_FlagSerialOrFlagLot). Thêm màn /Attribute, mở rộng form Product, API /api/products/{code} trả thêm trường master. Build Release 0 error, smoke test /Attribute=200 + API OK.
- 2026: Port nghiệp vụ **Quy cách + Bảng giá theo quy cách** từ `2019.4.ProductCenter` (Mst_Spec / Mst_SpecPrice — phần "quy cách, giá" của product master chưa có trong MiniPIM): thêm entity `Spec` (SpecCode/SpecName/ModelCode/SpecType1-2/Color/FlagHasSerial/FlagHasLOT/DefaultUnitCode/StandardUnitCode) và `SpecPrice` (khóa nghiệp vụ SpecCode+UnitCode, BuyPrice/SellPrice/CurrencyCode/VATRateCode/DiscountVND/EffectDTimeStart-End). Quy tắc nghiệp vụ port từ `Mst_SpecPrice_Create`/`Mst_SpecPrice_CheckDB`: quy cách phải tồn tại & đang dùng, mỗi cặp (SpecCode, UnitCode) chỉ 1 dòng giá, giá không âm. Thêm màn /Spec (CRUD quy cách) + /SpecPrice (bảng giá, lọc theo quy cách), API `/api/specs` và `/api/specs/{code}/prices`. Build Release 0 error; smoke test /Spec=200, /SpecPrice=200, API OK, và POST trùng giá bị chặn đúng thông báo.
- 2026: Port nghiệp vụ **Nhóm hàng phân cấp** từ `2019.4.ProductCenter` (Mst_ProductGroup — phần "danh mục" của product master chưa có trong MiniPIM, trước đây `ProductGroup` chỉ là 1 tầng phẳng): thêm trường phân cấp `ParentCode` + đường dẫn vật chất hóa `BUCode`/`BUPattern`/`Level` + `FlagFG`/`Active`. Quy tắc nghiệp vụ port từ `Mst_ProductGroup_CreateX`/`UpdateX`/`DeleteX`/`UpdBU`: mã & tên nhóm bắt buộc, mã/tên không trùng, nhóm cha phải tồn tại, không cho nhóm là cha của chính nó; sau mỗi lần lưu/xóa tự tính lại đường dẫn BU (gốc = mã, con = "cha.con", Level = cha.Level+1); không cho xóa nhóm nếu còn hàng hóa thuộc nhóm hoặc còn nhóm con. Thêm màn /Group (cây phân cấp + form Edit chọn nhóm cha) và nút xóa có guard. Build Release 0 error (đồng thời loại các file dump `_*.cs` khỏi compile trong csproj — chúng là bản sao nguồn ProductCenter dùng để đọc, không thuộc app và đang làm hỏng build).
- 2026: Port nghiệp vụ **Danh mục Đơn vị tính** từ `2019.4.ProductCenter` (Mst_Unit — danh mục ĐVT dùng chung cho sản phẩm/quy cách/bảng giá, trước đây MiniPIM chỉ lưu `Uom` dạng chuỗi tự do, chưa có danh mục): thêm entity `Unit` (Code = UnitCode hệ thống tự sinh, CodeUser = UnitCodeUser người dùng nhập, Name = UnitName, Remark, FlagActive→Active). Quy tắc nghiệp vụ port từ `Mst_Unit_CreateX`/`Mst_Unit_UpdateX`/`Mst_Unit_DeleteX`/`Mst_Unit_CheckDB_New20240108`/`Mst_Unit_CheckUnitCodeUser`/`Mst_Unit_CheckUnitName`: mã người dùng & tên ĐVT bắt buộc, mã hệ thống/mã người dùng/tên đều duy nhất trong tổ chức, khi sửa ĐVT phải tồn tại, khi xóa ĐVT phải tồn tại. Thêm màn /Unit (danh sách + tìm kiếm + form thêm/sửa/xóa), seed 6 ĐVT mẫu (Cái/Chiếc/Bộ/Kg/Mét/Thùng), thêm bảng `Units` vào migration Postgres. Build Release 0 error.- 2026: Port nghiệp vụ **Danh mục Thuế suất GTGT** từ `2019.4.ProductCenter` (Mst_VATRate — danh mục dùng chung cho product master, trước đây MiniPIM chỉ lưu `VatRateCode` dạng chuỗi tự do ở Product/SpecPrice, chưa có danh mục): thêm entity `VatRate` (Code = VATRateCode, Rate = VATRate %, Name = VATDesc, FlagActive→Active). Quy tắc nghiệp vụ port từ `Mst_VATRate_CheckDB`/`Mst_VATRate_Create`/`Mst_VATRate_Update`/`Mst_VATRate_Delete` (đọc từ nguồn C# + bảng mã lỗi ErrProductCenter): mã thuế suất & tên bắt buộc, mã không trùng, giá trị không âm, khi sửa/xóa phải tồn tại. Thêm màn /VatRate (danh sách + tìm kiếm + form thêm/sửa/xóa), seed 4 thuế suất mẫu (VAT0/VAT5/VAT8/VAT10), thêm bảng `VatRates` vào migration Postgres. Build Release 0 error.
